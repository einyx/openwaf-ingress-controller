{{- if .Values.modsec.enabled }}
kind: PersistentVolumeClaim
apiVersion: v1
metadata:
    name: modsec-logs
    annotations:
    volume.alpha.kubernetes.io/storage-class: default
spec:
    accessModes:
     - ReadWriteMany
    resources:
     requests:
       storage: 50G
{{- end }}
